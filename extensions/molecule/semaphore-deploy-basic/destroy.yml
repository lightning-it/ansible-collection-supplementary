---
- name: Destroy semaphore_deploy
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Nothing to destroy for semaphore-deploy-basic
      ansible.builtin.debug:
        msg: "No runtime resources created in semaphore-deploy-basic."
      changed_when: false
