---
- name: Converge vault_backup_restore
  hosts: localhost
  gather_facts: false
  vars:
    vault_deploy_host_ip: "127.0.0.1"
    vault_deploy_manage_systemd: false
    vault_deploy_backup_action: none
  roles:
    - role: vault_backup_restore
