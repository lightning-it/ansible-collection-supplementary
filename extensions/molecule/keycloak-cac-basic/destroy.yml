---
- name: Destroy keycloak_cac
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Nothing to destroy for keycloak-cac-basic
      ansible.builtin.debug:
        msg: "No runtime resources created in keycloak-cac-basic."
      changed_when: false
