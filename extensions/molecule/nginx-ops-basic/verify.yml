---
- name: Verify nginx_ops converge
  hosts: localhost
  gather_facts: false
  vars:
    nginx_ops_action: "none"
  tasks:
    - name: Assert converge ran
      ansible.builtin.assert:
        that:
          - nginx_ops_action is defined
