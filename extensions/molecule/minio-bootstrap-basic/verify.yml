---
- name: Verify minio_bootstrap converge
  hosts: localhost
  gather_facts: false
  vars:
    minio_bootstrap_skip: true
  tasks:
    - name: Assert converge ran
      ansible.builtin.assert:
        that:
          - minio_bootstrap_skip is defined
