---
- name: Destroy semaphore_cac
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Nothing to destroy for semaphore-cac-basic
      ansible.builtin.debug:
        msg: "No runtime resources created in semaphore-cac-basic."
      changed_when: false
