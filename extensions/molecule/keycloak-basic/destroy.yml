---
- name: Destroy keycloak_cac (legacy keycloak-basic)
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Nothing to destroy for keycloak-basic
      ansible.builtin.debug:
        msg: "No runtime resources created in keycloak-basic."
      changed_when: false
