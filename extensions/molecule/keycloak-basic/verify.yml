---
- name: Verify keycloak_cac (legacy keycloak-basic)
  hosts: localhost
  gather_facts: false
  vars:
    keycloak_cac_skip_apply: true
  tasks:
    - name: Assert keycloak_cac is in skip mode
      ansible.builtin.assert:
        that:
          - keycloak_cac_skip_apply | bool
