---
- name: Destroy forgejo_cac
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Nothing to destroy for forgejo-cac-basic
      ansible.builtin.debug:
        msg: "No runtime resources created in forgejo-cac-basic."
      changed_when: false
