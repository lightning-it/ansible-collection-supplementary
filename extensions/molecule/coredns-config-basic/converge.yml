---
- name: Converge coredns_config
  hosts: localhost
  gather_facts: false
  vars:
    coredns_deploy_health_url: "http://127.0.0.1:8080/health"
    coredns_deploy_skip_runtime: true
    coredns_deploy_skip_config: true
    coredns_config_manage: false
  roles:
    - role: coredns_config
