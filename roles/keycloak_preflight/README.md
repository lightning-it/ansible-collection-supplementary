# keycloak_preflight

Preflight checks for Keycloak lifecycle runs.

Checks include Podman, systemd, port availability, and optional Vault auth input
validation. See `roles/keycloak_preflight/defaults/main.yml`.
