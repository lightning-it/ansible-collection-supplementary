---
- name: Ensure MinIO directories exist
  ansible.builtin.file:
    path: "{{ item.path }}"
    state: directory
    owner: "{{ item.owner | default(minio_run_as_user) }}"
    group: "{{ item.group | default(minio_run_as_group) }}"
    mode: "{{ item.mode }}"
  loop:
    - path: "{{ minio_host_data_dir }}"
      mode: '0750'
    - path: "{{ minio_config_dir }}"
      mode: '0750'
    - path: "{{ minio_cert_dir }}"
      mode: '0750'
    - path: "{{ minio_pod_manifest_path | dirname }}"
      owner: root
      group: root
      mode: '0755'
  when: item.path | default('') | length > 0
  notify:
    - Recreate minio pod
    - Run minio pod
  tags:
    - deployment

- name: Render MinIO Pod manifest
  ansible.builtin.template:
    src: minio-pod.yml.j2
    dest: "{{ minio_pod_manifest_path }}"
    mode: '0644'
  notify:
    - Recreate minio pod
    - Run minio pod
  tags:
    - deployment

- name: Flush handlers
  ansible.builtin.meta: flush_handlers
  tags:
    - deployment
