---
- name: Prechecks
  ansible.builtin.import_tasks: assert.yml
  tags: always

- name: Resolve MinIO endpoints and credentials
  ansible.builtin.import_role:
    name: minio_foundational
  tags: always

- name: Deploy MinIO runtime
  ansible.builtin.include_tasks: deploy.yml
