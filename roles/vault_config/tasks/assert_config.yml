---
- name: Assert | Test config variables are defined
  ansible.builtin.assert:
    that:
      - vault_deploy_terraform_source is defined and vault_deploy_terraform_source | length > 0
      - vault_deploy_url | length > 0
    quiet: true
  tags: always
