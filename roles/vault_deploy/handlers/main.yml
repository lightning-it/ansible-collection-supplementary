---
- name: Recreate vault pod
  ansible.builtin.set_fact:
    vault_deploy_kubeplay_remove: true
  changed_when: false

- name: Run vault pod
  ansible.builtin.set_fact:
    vault_deploy_kubeplay_run: true
  changed_when: false
