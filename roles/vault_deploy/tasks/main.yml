---
- name: Prechecks
  ansible.builtin.import_tasks: assert.yml
  tags: always

- name: Token preflight checks
  ansible.builtin.include_tasks: preflight.yml
  tags: always

- name: Deploy Vault on RHEL
  ansible.builtin.include_tasks: deploy.yml
