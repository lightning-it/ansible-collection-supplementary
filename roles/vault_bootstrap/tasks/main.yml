---
- name: Ensure required Vault vars are defined
  ansible.builtin.import_role:
    name: vault_deploy
    tasks_from: assert
  tags: always

- name: Bootstrap Vault (init + unseal)
  ansible.builtin.include_tasks: bootstrap.yml
