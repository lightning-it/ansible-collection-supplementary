---
- name: Prechecks
  ansible.builtin.import_tasks: assert.yml
  tags: always

- name: Resolve MinIO endpoints
  ansible.builtin.import_tasks: resolve_endpoints.yml

- name: Resolve MinIO credentials
  ansible.builtin.import_tasks: resolve_credentials.yml
