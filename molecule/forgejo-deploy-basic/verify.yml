---
- name: Verify forgejo deploy syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.forgejo_deploy coverage stub ran
      ansible.builtin.assert:
        that:
          - forgejo_deploy_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
