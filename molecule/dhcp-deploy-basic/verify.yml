---
- name: Verify dhcp deploy syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.dhcp_deploy coverage stub ran
      ansible.builtin.assert:
        that:
          - dhcp_deploy_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
