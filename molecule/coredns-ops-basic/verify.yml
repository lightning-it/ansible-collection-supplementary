---
- name: Verify coredns ops syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.coredns_ops coverage stub ran
      ansible.builtin.assert:
        that:
          - coredns_ops_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
