---
- name: Verify cloudflared syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.cloudflared coverage stub ran
      ansible.builtin.assert:
        that:
          - cloudflared_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
