---
- name: Verify dhcp config syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.dhcp_config coverage stub ran
      ansible.builtin.assert:
        that:
          - dhcp_config_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
