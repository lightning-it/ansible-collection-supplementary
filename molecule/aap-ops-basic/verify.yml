---
- name: Verify aap ops syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.aap_ops coverage stub ran
      ansible.builtin.assert:
        that:
          - aap_ops_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
