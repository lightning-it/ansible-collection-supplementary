---
- name: Verify vault bootstrap syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.vault_bootstrap coverage stub ran
      ansible.builtin.assert:
        that:
          - vault_bootstrap_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
