---
- name: Verify minio validate syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.minio_validate coverage stub ran
      ansible.builtin.assert:
        that:
          - minio_validate_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
