---
- name: Verify keycloak deploy syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.keycloak_deploy coverage stub ran
      ansible.builtin.assert:
        that:
          - keycloak_deploy_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
