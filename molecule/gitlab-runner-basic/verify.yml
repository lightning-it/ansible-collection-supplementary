---
- name: Verify gitlab runner syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.gitlab_runner coverage stub ran
      ansible.builtin.assert:
        that:
          - gitlab_runner_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
