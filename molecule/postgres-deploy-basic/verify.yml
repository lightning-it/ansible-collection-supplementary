---
- name: Verify postgres deploy syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.postgres_deploy coverage stub ran
      ansible.builtin.assert:
        that:
          - postgres_deploy_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
