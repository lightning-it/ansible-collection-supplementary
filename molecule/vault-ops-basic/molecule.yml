---
dependency:
  name: shell
  command: ":"

driver:
  name: default

platforms:
  - name: localhost
    managed: false

provisioner:
  name: ansible
  inventory:
    host_vars:
      localhost:
        ansible_connection: local
  playbooks:
    converge: converge.yml
    verify: verify.yml
    cleanup: cleanup.yml

scenario:
  name: vault-ops-basic
  test_sequence:
    - dependency
    - syntax
    - converge
    - idempotence
    - verify
    - cleanup

verifier:
  name: ansible
