---
- name: Verify vault ops syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.vault_ops coverage stub ran
      ansible.builtin.assert:
        that:
          - vault_ops_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
