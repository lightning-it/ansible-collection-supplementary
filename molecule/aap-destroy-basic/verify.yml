---
- name: Verify aap destroy syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    aap_destroy_molecule_marker_path: >-
      {{ lookup('env', 'MOLECULE_EPHEMERAL_DIRECTORY') }}/aap-destroy-coverage-mode
  tasks:
    - name: Read lit.supplementary.aap_destroy coverage marker
      ansible.builtin.slurp:
        src: "{{ aap_destroy_molecule_marker_path }}"
      register: aap_destroy_molecule_marker

    - name: Assert lit.supplementary.aap_destroy coverage stub ran
      ansible.builtin.assert:
        that:
          - aap_destroy_molecule_marker.content | b64decode | trim == 'syntax_stub'
        quiet: true
