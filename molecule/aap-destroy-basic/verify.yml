---
- name: Verify aap destroy syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.aap_destroy coverage stub ran
      ansible.builtin.assert:
        that:
          - aap_destroy_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
