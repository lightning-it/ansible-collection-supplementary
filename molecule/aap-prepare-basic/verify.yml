---
- name: Verify aap prepare syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.aap_prepare coverage stub ran
      ansible.builtin.assert:
        that:
          - aap_prepare_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
