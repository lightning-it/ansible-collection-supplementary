---
- name: Converge minio_backup_restore
  hosts: localhost
  gather_facts: true
  vars:
    minio_root_user: "minioadmin"
    minio_root_password: "minioadmin"
    minio_skip_runtime: true
    minio_backup_action: "none"
  roles:
    - role: minio_backup_restore
