---
- name: Converge minio_bootstrap
  hosts: localhost
  gather_facts: true
  vars:
    minio_root_user: "minioadmin"
    minio_root_password: "minioadmin"
    minio_skip_runtime: true
    minio_skip_bootstrap: true
    minio_bootstrap_buckets: []
  roles:
    - role: minio_bootstrap
