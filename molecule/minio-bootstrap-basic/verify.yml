---
- name: Verify minio bootstrap syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.minio_bootstrap coverage stub ran
      ansible.builtin.assert:
        that:
          - minio_bootstrap_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
