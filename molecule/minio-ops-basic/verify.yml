---
- name: Verify minio ops syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.minio_ops coverage stub ran
      ansible.builtin.assert:
        that:
          - minio_ops_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
