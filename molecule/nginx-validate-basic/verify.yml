---
- name: Verify nginx validate syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.nginx_validate coverage stub ran
      ansible.builtin.assert:
        that:
          - nginx_validate_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
