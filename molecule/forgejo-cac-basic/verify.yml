---
- name: Verify forgejo cac syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.forgejo_cac coverage stub ran
      ansible.builtin.assert:
        that:
          - forgejo_cac_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
