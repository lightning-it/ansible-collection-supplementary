---
- name: Verify openvpn syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.openvpn coverage stub ran
      ansible.builtin.assert:
        that:
          - openvpn_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
