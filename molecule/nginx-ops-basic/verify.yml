---
- name: Verify nginx ops syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.nginx_ops coverage stub ran
      ansible.builtin.assert:
        that:
          - nginx_ops_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
