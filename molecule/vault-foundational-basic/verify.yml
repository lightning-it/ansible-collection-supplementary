---
- name: Verify vault foundational syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.vault_foundational coverage stub ran
      ansible.builtin.assert:
        that:
          - vault_foundational_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
