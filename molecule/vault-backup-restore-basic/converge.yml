---
- name: Converge vault backup restore syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    vault_backup_restore_molecule_execute_role: false
  tasks:
    - name: Import lit.supplementary.vault_backup_restore for syntax coverage
      ansible.builtin.import_role:
        name: lit.supplementary.vault_backup_restore
      when: vault_backup_restore_molecule_execute_role | bool

    - name: Record lit.supplementary.vault_backup_restore Molecule coverage mode
      ansible.builtin.set_fact:
        vault_backup_restore_molecule_coverage_mode: syntax_stub
