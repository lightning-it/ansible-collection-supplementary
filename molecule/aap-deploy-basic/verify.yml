---
- name: Verify aap deploy syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.aap_deploy coverage stub ran
      ansible.builtin.assert:
        that:
          - aap_deploy_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
