---
- name: Verify vault config syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.vault_config coverage stub ran
      ansible.builtin.assert:
        that:
          - vault_config_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
