---
- name: Converge vault
  hosts: localhost
  gather_facts: true
  vars:
    cluster_id: test-cluster
    vault_terraform_source: "."
    vault_url: "https://vault.example.test:8200"
    vault_manage_systemd: false
    vault_skip_runtime: true
  roles:
    - role: vault_deploy
