---
- name: Verify dhcp validate syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.dhcp_validate coverage stub ran
      ansible.builtin.assert:
        that:
          - dhcp_validate_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
