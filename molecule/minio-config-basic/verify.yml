---
- name: Verify minio_config converge
  hosts: localhost
  gather_facts: false
  vars:
    minio_config_skip_config: true
  tasks:
    - name: Assert converge ran
      ansible.builtin.assert:
        that:
          - minio_config_skip_config is defined
