---
- name: Verify minio_config converge
  hosts: localhost
  gather_facts: false
  vars:
    minio_skip_config: true
  tasks:
    - name: Assert converge ran
      ansible.builtin.assert:
        that:
          - minio_skip_config is defined
