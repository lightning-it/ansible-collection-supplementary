---
- name: Verify minio config syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.minio_config coverage stub ran
      ansible.builtin.assert:
        that:
          - minio_config_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
