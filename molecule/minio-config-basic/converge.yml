---
- name: Converge minio_config
  hosts: localhost
  gather_facts: true
  vars:
    minio_root_user: "minioadmin"
    minio_root_password: "minioadmin"
    minio_skip_runtime: true
    minio_skip_config: true
    minio_config_users: []
  roles:
    - role: minio_config
