---
- name: Verify nexus syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.nexus coverage stub ran
      ansible.builtin.assert:
        that:
          - nexus_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
