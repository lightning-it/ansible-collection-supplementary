---
- name: Verify coredns deploy syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.supplementary.coredns_deploy coverage stub ran
      ansible.builtin.assert:
        that:
          - coredns_deploy_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
