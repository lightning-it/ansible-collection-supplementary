---
bugfixes:
  - >-
    Bind main-promotion review to the exact current protected controller and invoke the protected verifier helper
    from the same immutable workflow revision instead of dispatching through a mutable branch ref.
  - >-
    Preserve fail-closed forward-proxy Tiny JUnit evidence when convergence fails before verification starts.
