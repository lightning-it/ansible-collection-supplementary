---
bugfixes:
  - >-
    Preserve a fail-closed Tiny JUnit artifact even when forward-proxy
    verification aborts before the final per-contract report is assembled,
    without publishing successful test counters for an incomplete run.
  - >-
    Bind forward-proxy rollback handlers to the explicit first mutation
    boundary after read-only preflight, including failures that occur inside
    a mutating operation.
