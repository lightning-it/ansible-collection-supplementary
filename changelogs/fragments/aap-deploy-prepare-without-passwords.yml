---
bugfixes:
  - >-
    Skip effective installer admin-password validation when
    ``aap_deploy_run_installer`` is disabled, allowing prepare-only deployment
    runs without loading unused installer secrets.
