---
bugfixes:
  - >-
    Keep Release-App failed-release recovery pull requests on the normal protected
    update-branch path while dispatching the Exact-Revision review only after the
    final App-bound head, base, ancestry, path manifest, and diff are revalidated.
