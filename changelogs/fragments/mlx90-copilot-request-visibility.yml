---
bugfixes:
  - >-
    Keep the final exact-head Copilot reviewer request idempotent while waiting
    for GitHub reviewer propagation, preventing a valid frozen review from
    failing only because visibility takes longer than the short API window.
